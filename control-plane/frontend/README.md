# Technologies used

this is a base project using vite, react, typescript

using tools:

- tailwind for styling
- radix-ui/shadcn for builtin components (found inside components/ui)
- react-router for routing
- react redux for state management
- react toastify for toast popups
- zod for validation
- axios for api integration
