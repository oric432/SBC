name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-lint-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y clang clang-tidy clang-format ninja-build

      - name: Cache FetchContent dependencies
        uses: actions/cache@v4
        with:
          path: build/_deps
          key: deps-${{ runner.os }}-debug-${{ hashFiles('third_party/**/declare.cmake', 'third_party/**/configure.cmake') }}
          restore-keys: |
            deps-${{ runner.os }}-debug-

      - name: Configure (debug preset, clang-tidy on)
        run: cmake --preset debug

      - name: Build
        run: cmake --build --preset build-debug

      - name: Run unit tests
        run: ctest --preset test-debug --output-on-failure

      - name: Check formatting (non-mutating)
        run: cmake --build build --target format-check
