# SBC Project

## Quick Start with Just

To see all available root commands (e.g. running all components together), run:
```bash
just
```

To quickly setup and start all components concurrently in development mode:
```bash
just setup-all
just run-dev-all
```
